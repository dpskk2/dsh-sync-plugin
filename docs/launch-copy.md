# 发布素材（待发布）

以下文案用于本轮改动发布后。不要在代码与文档尚未上线时宣称用户已经可以使用新版功能。

## GitHub About

换台电脑，接着做。通过自己的 GitHub 仓库同步 DeepSeek Harness 会话、附件、设置与工作区文件。DSH sync across computers.

## 更新说明

### 让两台电脑接上同一份 DSH 工作

这次更新集中改进首次使用：README 补齐了从第一台上传到第二台取回的全过程；设置页可以选择手动或自动同步，也能关闭工作区文件上传。保存后的生效时间、同步范围和常见错误都有对应说明。

同时增加英文入门文档。默认仍是手动同步，专用凭据文件 `.credentials.yaml` 排除同步；每台电脑需要单独配置 API 密钥。设置页切换自动模式保存即生效，无需重启 DSH。

## 社区介绍

在台式机上用 DSH 做了一半，换到笔记本还得搬会话、项目文件和配置？

我做了 DSH Sync：通过自己的 GitHub 仓库，把 DeepSeek Harness 的会话、附件、设置和工作区文件同步到另一台电脑。支持手动和自动同步，也可以只同步会话与设置。

安装：

```sh
dsh plugin --profile web add dsh-sync-plugin
```

重启 DSH，在同一系统用户下登录 GitHub CLI，然后点侧栏「⟳ 同步」。完整教程包含第二台电脑的接入和双向验证。

适合个人多台电脑交替使用；建议用私有仓库，API 密钥在每台机器单独配置。

项目：https://github.com/dpskk2/dsh-sync-plugin

欢迎反馈首次安装卡在哪一步，或你最想同步的内容。
